import React from 'react'
import NavItems from '../navItems'

const Nav = () =>
{    
    return(
        <nav className="nav">
            <ul className="list">
                <NavItems />
            </ul>
        </nav>
    )
}

export default Nav